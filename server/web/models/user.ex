defmodule App.User do
  def insert(data) do
    IO.puts "Stub #{data}"
  end

  def update(id, query) do
    IO.puts "Stub #{{id, query}}"
  end

  def delete(id) do
    IO.puts "Stub #{id}"
  end
end
